# Common core dependencies — shared by every backend.
#
# Backend-specific packages live in separate files (so a vLLM-only install
# never pulls in the ollama package and vice versa):
#   pip install -r requirements-ollama.txt   # default backend (Ollama)
#   pip install -r requirements-vllm.txt     # vLLM / OpenAI-compatible server
#
# GPU/ML packages for image generation and MediaAnalyst persona
# are in requirements-gpu.txt. Install only if needed:
#   pip install -r requirements-gpu.txt

requests>=2.31.0
Pillow>=10.0.0
beautifulsoup4>=4.12.0

# Development / testing
pytest>=7
pytest-cov>=4
